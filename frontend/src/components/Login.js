import React from 'react';

function Login({ setAuth, setAuthToken }) {
  // Add login logic here
  return (
    <div>
      <h2>Login</h2>
      {/* Add login form */}
    </div>
  );
}

export default Login;
